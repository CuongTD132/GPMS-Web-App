type SemiFinishedProduct = {
    code: string;
    name: string;
    quantity: number;
    description?: string;
};

type SemiFinishedProductInput = {
    code: string;
    name: string;
    quantity: number;
    description?: string;
};
